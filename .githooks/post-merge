#!/usr/bin/env bash
# Post-merge hook to restart the bot after a merge (useful in deployments)
cd "$(git rev-parse --show-toplevel)" || exit 0
if [ -f scripts/restart_bot.sh ]; then
  scripts/restart_bot.sh >/dev/null 2>&1 &
fi
