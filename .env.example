# .env.example
# Copy this file to `.env` and fill in real secret values.
# Keep `.env` out of source control and never share your keys.

# Required: Telegram bot token (format: 123456:ABC-...)
BOT_TOKEN=

# Optional but recommended: OpenAI API key (sk-...)
OPENAI_API_KEY=

# Optional GNews API key. If empty, the bot will fall back to RSS scraping.
GNEWS_API_KEY=

# Optional admin ids (comma-separated Telegram user IDs) allowed to access /stats
# Example: ADMIN_ID=123456789,987654321
ADMIN_ID=

# Notes:
# - After copying to .env, run the bot locally with a Python virtualenv active:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#   python penpal_english_bot.py
